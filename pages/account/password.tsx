// Components
import Password from '@/components/account/password';
import AccountLayout from '@/components/account/pageLayout';

export default function PasswordPage(): JSX.Element {
    return <AccountLayout component={Password} />;
}
