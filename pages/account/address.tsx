// Components
import Address from '@/components/account/address';
import AccountLayout from '@/components/account/pageLayout';

export default function AddressPage(): JSX.Element {
    return <AccountLayout component={Address} />;
}
